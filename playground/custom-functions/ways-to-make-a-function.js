console.log('it works!')

const inchesToCM = (inches) => inches * 2.54;

const add = (a, b=3) => a + b;

const makeABaby = (first, last) => ({name: `${first} ${last}`, age: 0})


//IIFE
// Immediately Invoiced Function Expression

(function() {
    return 'You are cool';
})();

//Methods!!
// console is a object, log is an function that lives inside of an object, or a method!

